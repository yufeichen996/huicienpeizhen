import { hospitals } from '../mocks/hospitals'
export const hospitalService={list(options:{query?:string;district?:string;level?:string;popularOnly?:boolean}={}){const q=(options.query||'').trim().toLowerCase();return hospitals.filter(i=>(!q||`${i.name}${i.shortName}${i.district}${i.address}${i.departmentGroups.flatMap(g=>g.departments).join('')}`.toLowerCase().includes(q))&&(!options.district||options.district==='all'||i.district===options.district)&&(!options.level||options.level==='all'||i.level===options.level)&&(!options.popularOnly||i.isPopular)).map(i=>({...i}))},getById(id:string){const item=hospitals.find(i=>i.id===id);return item?{...item}:undefined},districts(){return ['all',...new Set(hospitals.map(i=>i.district))]}}
