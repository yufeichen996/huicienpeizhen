<view class="card"><view class="head"><text>我的订单</text><text bindtap="all">查看全部 ›</text></view><view class="items"><view class="item tap-feedback" wx:for="{{items}}" wx:key="filter" data-filter="{{item.filter}}" bindtap="tap"><view class="icon"><app-icon name="{{item.icon}}" size="20" label="{{item.title}}"/></view><text>{{item.title}}</text><text wx:if="{{item.count}}" class="badge">{{item.count}}</text></view></view></view>
