server {
    listen 80;
    listen [::]:80;
    server_name api.huicien.com org.huicien.com admin.huicien.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
    }

    location / {
        return 503;
    }
}
